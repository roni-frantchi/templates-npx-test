version: 1

deploy:
  steps:
    terraformApply:
      before:
        - cd /mnt/efs && ls -n
        - mkdir /mnt/efs/private
        - chmod 700 /mnt/efs/private
        - chown 2000:2000 /mnt/efs/private
        - touch /mnt/efs/private
